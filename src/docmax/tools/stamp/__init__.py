"""Draw one PDF's first page onto the pages of another.

The overlay is a document, not an image and not a string of text -- see
``local.py`` for why that is the whole of the tool, and what ``watermark`` is
for instead.
"""

from __future__ import annotations
