"""Assemble image files into a single PDF, one page per image.

The second tool that takes several inputs, after ``merge`` -- which is what
makes ``-o`` pointing at one of the images a refusal rather than a silent loss.
See ``local.py``.
"""

from __future__ import annotations
